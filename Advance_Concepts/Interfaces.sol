// SPDX-License-Identifier: GPL-3.0

pragma solidity >=0.7.0 <0.9.0;

interface Interfaces {
    //For default here (into interfaces) the functions are vertuals, so that you don't need add it that to those
    function SumInterface(uint256 number1, uint256 number2) external returns (uint256);
}
